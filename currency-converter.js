const axios = require('axios');

const convertCurrency = async (fromCurrency, toCurrency, amount) => {
    // Get exchange rate
    // Get countries I can spend my money in
    // Return the initial amount and currency, the exchanged amount and currency and the countires my money will work in

    return '';
}

convertCurrency('GBP', 'EUR', 206);